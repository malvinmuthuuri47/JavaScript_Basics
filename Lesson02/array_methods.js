// Array methods (push, pop, shift, unshift)

let fruits = ['banana', 'apple', 'grapes'];

// push - Adds a new element at the end of an array
// fruits.push('Melon', 'Orange');

// pop - Removes the last element in the array
// fruits.pop();

// shift - Removes the first element in the array and returns it.
// let removedFruit = fruits.shift();
// console.log(removedFruit);

// unshift - Adds an element at the first position of the array
fruits.unshift('Grapes');


console.log(fruits);