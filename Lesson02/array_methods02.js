// Array useful methods

// indexof
// includes
// slice
// join
// concat

let fruits = ['banana', 'orange', 'apple', 'watermelon'];

// console.log(fruits.length);
// console.log(Array.isArray(fruits));

// indexof - finds the index of a specific element in an array
// let appleIdx = fruits.indexOf('apple');
// console.log(appleIdx);

// includes - checks if an element is present in an array
// let melon = fruits.includes('watermelon');
// console.log(melon);

// slice - Returns selected elements from the startIdx to the (EndIdx - 1) as a new array
// let selectedFruits = fruits.slice(1, 3);
// console.log(selectedFruits);

// join - Creates a string by joining all elements of an array using the specified separator
// let joinedFruits = fruits.join('  ');
// console.log(typeof(joinedFruits));
// console.log(joinedFruits);

// concat - allows the marging of arrays without modifying original arrays
// let numbers = [12, 13, 14, 15];

// let mixedArrays = fruits.concat(numbers);
// console.log(typeof(mixedArrays));
// console.log(mixedArrays);